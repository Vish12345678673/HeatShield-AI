import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.heatshieldai.app",
  appName: "HeatShield AI",
  webDir: "dist/client",
  android: {
    backgroundColor: "#070B16",
  },
};

export default config;
